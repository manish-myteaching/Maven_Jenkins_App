apiVersion: v1
kind: Service
metadata:
  name: svc-my-maven-app-dev  # Change this for staging/prod
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: my-maven-app
